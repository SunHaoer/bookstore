<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="pro.sunhao.bookstore.dao.CartDao">

    <select id="selectCountByProductId" resultType="Integer">
		select product_num from user#{userId}_cart
		where product_id=#{productId}
	</select>

    <insert id="insertProductById">
        insert into user1_cart
        (product_id, product_num)
        VALUES
        (#{productId}, 1);
    </insert>

    <update id="addProductById">
        update user#{userId}_cart
        set product_num=product_num+1
        where product_id=#{productId}
    </update>

    <delete id="deleteProductById">
        delete from user#{userId}_cart
        where product_id=#{productId}
    </delete>

    <update id="subProductById">
        update user#{userId}_cart
        set product_num=product_num-1
        where product_id=#{productId}
    </update>

    <select id="getProductCartViewModel" resultType="pro.sunhao.bookstore.viewmodel.ProductCartViewModel">
        select product_base.product_id, product_base.product_name, product_base.product_price, product_base.product_image_path, user1_cart.product_num
        from user1_cart left join product_base
        on user1_cart.product_id=product_base.product_id
    </select>

</mapper>